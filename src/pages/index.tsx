import type { NextPage } from "next";
import { Box } from "@mui/material";
import { Header } from "../components/Header";

const Home: NextPage = () => {
  return (
    <Box>
      <Header />
    </Box>
  );
};

export default Home;
